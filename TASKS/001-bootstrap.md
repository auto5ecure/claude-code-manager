# TASK 001: Bootstrap Project

## Objective
Set up working Electron + React + TypeScript development environment

## Acceptance Criteria
- [ ] `npm run dev` starts app with hot reload
- [ ] `npm run build` produces distributable
- [ ] TypeScript strict mode, no errors
- [ ] Basic window renders React component

## Steps
1. Create package.json with dependencies
2. Configure TypeScript (main + renderer)
3. Create Electron main process entry
4. Create React renderer entry
5. Configure Vite for renderer bundling
6. Test dev + build commands

## Status
DONE
